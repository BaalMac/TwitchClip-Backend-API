# Timetrack-WebServer
A Flask web app that tracks events triggered by a Discord bot command. Logs UTC timestamps and source links to a PostgreSQL database and displays the last triggered event with elapsed time on a live webpage.
